import { Options } from 'prettier';

const config: Options = {
    trailingComma: 'es5',
    printWidth: 120,
    tabWidth: 4,
    singleQuote: true,
    arrowParens: 'avoid',
};

export default config;
